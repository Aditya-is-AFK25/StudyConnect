// Layout.jsx — Shared Component
// Wraps all pages with consistent structure
// ⚠️ Discuss before editing

import React from "react";

function Layout({ children }) {
  return (
    <main>
      {children}
    </main>
  );
}

export default Layout;
