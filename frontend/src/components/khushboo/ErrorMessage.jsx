// ErrorMessage.jsx — Khushboo
// Error state UI

import React from "react";

function ErrorMessage({ message }) {
  return (
    <div>
      <p>Error: {message}</p>
      {/* TODO: Build error message UI here */}
    </div>
  );
}

export default ErrorMessage;
