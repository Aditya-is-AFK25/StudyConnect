// SessionCard.jsx — Khushboo
// Displays a single study session

import React from "react";

function SessionCard({ session }) {
  return (
    <div>
      <h3>{session?.title}</h3>
      {/* TODO: Build session card UI here */}
    </div>
  );
}

export default SessionCard;
