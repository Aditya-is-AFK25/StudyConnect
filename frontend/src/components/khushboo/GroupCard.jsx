// GroupCard.jsx — Khushboo
// Displays a single study group

import React from "react";

function GroupCard({ group }) {
  return (
    <div>
      <h3>{group?.name}</h3>
      {/* TODO: Build group card UI here */}
    </div>
  );
}

export default GroupCard;
